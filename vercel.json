{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "functions": {
    "api/webhook.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/webhook", "destination": "/api/webhook" }
  ]
}
